<h1>Venues</h1>
<% @venues.each do |venue| %>
  <div id='venue-<%= venue.id %>'>
    <h3>Name: <%= venue.name %></h3>
    <p>Capacity: <%= venue.capacity %></p>
    <p>Indoor: <%= venue.indoor %></p>
  </div>
<% end %>
